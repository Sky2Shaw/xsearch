function_annotation:
  function_name: FlashDecode
  qualified_name: FiaKernelNonQuant<FIAT>::FlashDecode
  canonical_name: FiaKernelNonQuant<FIAT>::FlashDecode
  owner: FiaKernelNonQuant
  owner_qualified: FiaKernelNonQuant<FIAT>
  owner_template_args:
  - FIAT
  template_params:
  - template<typename FIAT>
  variant: flash_decode
  stage: flash_decode
  file: op_kernel/fia_kernel_nonquant_sink.h
  line_range:
    start: 722
    end: 761
  role: FlashDecode setup, workspace sizing, or merge helper.
  confidence: high
  code_behavior:
    summary: FlashDecode setup, workspace sizing, or merge helper.
    key_steps:
    - Reads FD metadata arrays from metadataGm.
    - Builds FDparams for fdService.FlashDecode.
    - Initializes FD buffers and event IDs around the merge post-pass.
  dataflow:
    inputs:
    - FiaKernelNonQuant<FIAT>::FlashDecode
    - RunInfo/ConstInfo or metadata as visible in signature/body
    outputs:
    - stage outputs in GM/UB/L1 according to function body
    gm:
      reads:
      - GM tensors referenced in function body
      writes:
      - unknown
    ub:
      reads:
      - unknown
      writes:
      - unknown
    l1:
      reads:
      - unknown
      writes:
      - unknown
    l0:
      l0a:
      - unknown
      l0b:
      - unknown
      l0c:
      - unknown
  control_flow:
    loops:
    - for
    branches:
    - if
  pipeline:
    stages:
    - flash_decode
    - flash_decode
    - calls:InitBuffers,ICachePreLoad,SyncAll,GetValue,GetBaseMetaAbsIndex,GetAICMetaAbsIndex
    sync:
    - SyncAll
  memory_behavior:
    buffers:
    - unknown
    l1_residency:
    - unknown
    l1_partition:
    - unknown
  tiling_and_shape:
    fields:
    - unknown
    shape_assumptions:
    - 'source line evidence: op_kernel/fia_kernel_nonquant_sink.h:722-761'
  workspace:
    layout:
    - unknown
    offsets:
    - unknown
  optimization_intent:
  - Stage-specific tiling and buffer reuse reduce GM/L1 traffic.
  - Explicit synchronization exposes producer/consumer lifetime.
  tunable_or_structural_fields: []
  constraints:
  - unknown
  risks_if_changed:
  - unknown
  possible_dsl_sections:
  - flash_decode.merge
  suggested_dsl_fields:
  - path: flash_decode.merge.source_function
    meaning: Lowering evidence from FiaKernelNonQuant<FIAT>::FlashDecode
    evidence: op_kernel/fia_kernel_nonquant_sink.h:722-761
    confidence: high
  lowering_hints:
  - Preserve owner/template identity when lowering same-named stage functions.
  - Treat synchronization and workspace offsets as part of the stage contract.
  open_questions:
  - unknown
