function_annotation:
  function_name: SoftmaxFlashV2Compute
  qualified_name: FiaBlockVecNonQuant<FIAT>::SoftmaxFlashV2Compute
  canonical_name: FiaBlockVecNonQuant<FIAT>::SoftmaxFlashV2Compute
  owner: FiaBlockVecNonQuant
  owner_qualified: FiaBlockVecNonQuant<FIAT>
  owner_template_args:
  - FIAT
  template_params:
  - template<typename FIAT>
  variant: nonquant
  stage: softmax
  file: op_kernel/fia_block_vec_nonquant_sink.h
  line_range:
    start: 527
    end: 562
  role: nonquant vector stage for softmax, including UB movement, softmax/output handling, or split-KV export when present.
  confidence: medium
  code_behavior:
    summary: nonquant vector stage for softmax, including UB movement, softmax/output handling, or split-KV export when present.
    key_steps:
    - 'Calls: SoftMaxFlashV2TilingFunc, GetSize.'
    - Touches online softmax state or LSE outputs.
  dataflow:
    inputs:
    - FiaBlockVecNonQuant<FIAT>::SoftmaxFlashV2Compute
    - RunInfo/ConstInfo or metadata as visible in signature/body
    outputs:
    - stage outputs in GM/UB/L1 according to function body
    gm:
      reads:
      - unknown
      writes:
      - unknown
    ub:
      reads:
      - LocalTensor/UB operands
      writes:
      - LocalTensor/UB temporaries
    l1:
      reads:
      - unknown
      writes:
      - unknown
    l0:
      l0a:
      - unknown
      l0b:
      - unknown
      l0c:
      - unknown
  control_flow:
    loops:
    - unknown
    branches:
    - if
    - else
  pipeline:
    stages:
    - softmax
    - nonquant
    - calls:SoftMaxFlashV2TilingFunc,GetSize
    sync:
    - unknown
  memory_behavior:
    buffers:
    - LocalTensor
    l1_residency:
    - unknown
    l1_partition:
    - unknown
  tiling_and_shape:
    fields:
    - preLoadNum
    shape_assumptions:
    - 'source line evidence: op_kernel/fia_block_vec_nonquant_sink.h:527-562'
  workspace:
    layout:
    - unknown
    offsets:
    - uint32_t baseOffset = mSplitInfo.nBufferStartM / 2 + startRow;
    - baseOffset = baseOffset * this->brcbNum;
    - uint32_t softmaxOutOffset = outIdx * SOFTMAX_TMP_BUFFER_SIZE / sizeof(COMPUTE_T) + baseOffset;
    - inMaxTensor = softmaxMaxUb[inIdx * SOFTMAX_TMP_BUFFER_SIZE / sizeof(COMPUTE_T) + baseOffset];
    - inSumTensor = softmaxSumUb[inIdx * SOFTMAX_TMP_BUFFER_SIZE / sizeof(COMPUTE_T) + baseOffset];
    - mmResUb, softmaxSumUb[softmaxOutOffset], softmaxMaxUb[softmaxOutOffset], mmResUb,
  optimization_intent:
  - Stage-specific tiling and buffer reuse reduce GM/L1 traffic.
  - Explicit synchronization exposes producer/consumer lifetime.
  tunable_or_structural_fields:
  - name: preLoadNum
    role: shape, stride, or pipeline structural field
    evidence: op_kernel/fia_block_vec_nonquant_sink.h:527-562
  constraints:
  - unknown
  risks_if_changed:
  - unknown
  possible_dsl_sections:
  - pipeline.vector_stages
  - softmax.online_state
  suggested_dsl_fields:
  - path: pipeline.vector_stages.source_function
    meaning: Lowering evidence from FiaBlockVecNonQuant<FIAT>::SoftmaxFlashV2Compute
    evidence: op_kernel/fia_block_vec_nonquant_sink.h:527-562
    confidence: medium
  - path: softmax.online_state.source_function
    meaning: Lowering evidence from FiaBlockVecNonQuant<FIAT>::SoftmaxFlashV2Compute
    evidence: op_kernel/fia_block_vec_nonquant_sink.h:527-562
    confidence: medium
  lowering_hints:
  - Preserve owner/template identity when lowering same-named stage functions.
  - Treat synchronization and workspace offsets as part of the stage contract.
  open_questions:
  - No additional source-local question identified.
