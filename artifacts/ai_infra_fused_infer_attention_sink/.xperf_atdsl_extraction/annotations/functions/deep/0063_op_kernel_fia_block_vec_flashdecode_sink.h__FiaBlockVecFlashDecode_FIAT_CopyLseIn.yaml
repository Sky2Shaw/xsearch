function_annotation:
  function_name: CopyLseIn
  qualified_name: FiaBlockVecFlashDecode<FIAT>::CopyLseIn
  canonical_name: FiaBlockVecFlashDecode<FIAT>::CopyLseIn
  owner: FiaBlockVecFlashDecode
  owner_qualified: FiaBlockVecFlashDecode<FIAT>
  owner_template_args:
  - FIAT
  template_params:
  - template<typename FIAT>
  variant: flash_decode
  stage: flash_decode
  file: op_kernel/fia_block_vec_flashdecode_sink.h
  line_range:
    start: 229
    end: 254
  role: FlashDecode setup, workspace sizing, or merge helper.
  confidence: medium
  code_behavior:
    summary: FlashDecode setup, workspace sizing, or merge helper.
    key_steps:
    - 'Calls: DataCopy.'
    - Uses DataCopy/DataCopyPad for GM/L1/UB movement.
    - Coordinates pipeline state with event or cross-core flags.
  dataflow:
    inputs:
    - FiaBlockVecFlashDecode<FIAT>::CopyLseIn
    - RunInfo/ConstInfo or metadata as visible in signature/body
    outputs:
    - stage outputs in GM/UB/L1 according to function body
    gm:
      reads:
      - GM tensors referenced in function body
      writes:
      - DataCopy/Fixpipe destinations visible in body
    ub:
      reads:
      - LocalTensor/UB operands
      writes:
      - LocalTensor/UB temporaries
    l1:
      reads:
      - unknown
      writes:
      - unknown
    l0:
      l0a:
      - unknown
      l0b:
      - unknown
      l0c:
      - unknown
  control_flow:
    loops:
    - for
    branches:
    - unknown
  pipeline:
    stages:
    - flash_decode
    - flash_decode
    - calls:DataCopy
    sync:
    - WaitFlag
    - SetFlag
  memory_behavior:
    buffers:
    - LocalTensor
    l1_residency:
    - unknown
    l1_partition:
    - unknown
  tiling_and_shape:
    fields:
    - unknown
    shape_assumptions:
    - 'source line evidence: op_kernel/fia_block_vec_flashdecode_sink.h:229-254'
  workspace:
    layout:
    - unknown
    offsets:
    - uint32_t dealRowCount, uint64_t baseOffset, uint32_t cntM)
    - uint64_t combineLseOffset = (baseOffset + startRow) * fa_base_vector::FP32_BLOCK_ELEMENT_NUM;
    - uint64_t combineLoopOffset = constInfo.mBaseSize * fa_base_vector::FP32_BLOCK_ELEMENT_NUM;
    - DataCopy(lseSum[i * dealRowCountAlign], lseSumFdGm[combineLseOffset + i * combineLoopOffset],
    - "dealRowCountAlign); // \u4EFD\u6570offset"
    - DataCopy(lseMax[i * dealRowCountAlign], lseMaxFdGm[combineLseOffset + i * combineLoopOffset],
  optimization_intent:
  - Stage-specific tiling and buffer reuse reduce GM/L1 traffic.
  - Explicit synchronization exposes producer/consumer lifetime.
  tunable_or_structural_fields:
  - name: mBaseSize
    role: shape, stride, or pipeline structural field
    evidence: op_kernel/fia_block_vec_flashdecode_sink.h:229-254
  constraints:
  - Event wait/set ordering is part of correctness, not only performance.
  risks_if_changed:
  - Missing event release can stall producer/consumer queues.
  possible_dsl_sections:
  - flash_decode.merge
  suggested_dsl_fields:
  - path: flash_decode.merge.source_function
    meaning: Lowering evidence from FiaBlockVecFlashDecode<FIAT>::CopyLseIn
    evidence: op_kernel/fia_block_vec_flashdecode_sink.h:229-254
    confidence: medium
  lowering_hints:
  - Preserve owner/template identity when lowering same-named stage functions.
  - Treat synchronization and workspace offsets as part of the stage contract.
  open_questions:
  - No additional source-local question identified.
